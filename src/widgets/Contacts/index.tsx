import classes from './index.module.scss';

const Contacts = () => {
    return ( 
        <div className={classes.chat_contacts}>
            CONTACTS
        </div>
     );
}
 
export default Contacts;