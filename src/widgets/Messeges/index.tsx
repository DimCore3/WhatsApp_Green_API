import classes from './index.module.scss';

const Messeges = () => {
    return (
        <div className={classes.chat_messeges}>
            MESSEGES
        </div>
    );
}

export default Messeges;